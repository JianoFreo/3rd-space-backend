import {Router} from 'express';import {auth} from '../middlewares/auth.middleware.js';import {me,registrations,rewards} from '../controllers/user/getRequests.controller.js';import {asyncHandler} from '../utils/asyncHandler.js';const router=Router();router.use(auth);router.get('/me',asyncHandler(me));router.get('/registrations',asyncHandler(registrations));router.get('/rewards',asyncHandler(rewards));export default router;
