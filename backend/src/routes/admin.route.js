import {Router} from 'express';import {auth} from '../middlewares/auth.middleware.js';import {adminOnly} from '../middlewares/admin.middleware.js';import {dashboard,pendingEvents,users} from '../controllers/admin/getRequests.controller.js';import {approveEvent} from '../controllers/admin/updateRequests.controller.js';import {asyncHandler} from '../utils/asyncHandler.js';const router=Router();router.use(auth,adminOnly);router.get('/dashboard',asyncHandler(dashboard));router.get('/events/pending',asyncHandler(pendingEvents));router.patch('/events/:id/approval',asyncHandler(approveEvent));router.get('/users',asyncHandler(users));export default router;
