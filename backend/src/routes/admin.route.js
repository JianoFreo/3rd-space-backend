import {Router} from 'express';import {auth} from '../middlewares/auth.middleware.js';import {admin} from '../middlewares/admin.middleware.js';import {dashboard,pendingEvents} from '../controllers/admin/getRequests.controller.js';import {approveEvent} from '../controllers/admin/updateRequests.controller.js';const r=Router();r.use(auth,admin);r.get('/dashboard',dashboard);r.get('/events/pending',pendingEvents);r.patch('/events/:id/approval',approveEvent);export default r;
