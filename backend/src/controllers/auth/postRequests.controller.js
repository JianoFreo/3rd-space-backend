import bcrypt from 'bcryptjs';import {supabase} from '../../config/db.js';import {signToken} from '../../utils/jwt.js';
export async function register(req,res){try{const{name,email,password,role='user'}=req.body;if(!name||!email||!password)return res.status(400).json({message:'Name, email and password are required'});if(!['user','organizer'].includes(role))return res.status(400).json({message:'Invalid role'});const {data:existing}=await supabase.from('users').select('id').eq('email',email.toLowerCase()).maybeSingle();if(existing)return res.status(409).json({message:'Email already registered'});const password_hash=await bcrypt.hash(password,12);const {data:user,error}=await supabase.from('users').insert({name,email:email.toLowerCase(),password_hash,role}).select('id,name,email,role,img_url,created_at').single();if(error)throw error;res.status(201).json({user,token:signToken(user)})}catch(e){res.status(500).json({message:e.message})}}
export async function login(req,res){try{const{email,password}=req.body;const {data:user,error}=await supabase.from('users').select('*').eq('email',email?.toLowerCase()).single();if(error||!user||!(await bcrypt.compare(password||'',user.password_hash)))return res.status(401).json({message:'Invalid email or password'});const safe={id:user.id,name:user.name,email:user.email,role:user.role,img_url:user.img_url,created_at:user.created_at};res.json({user:safe,token:signToken(safe)})}catch(e){res.status(500).json({message:e.message})}}
