import {supabase} from '../../config/db.js';export async function approveEvent(req,res){const approval_status=req.body.approval_status;if(!['approved','rejected'].includes(approval_status))return res.status(400).json({message:'Invalid approval status'});const {data,error}=await supabase.from('events').update({approval_status,approved_by:req.user.id}).eq('id',req.params.id).select().single();if(error)return res.status(500).json({message:error.message});res.json(data)}
