import {sql} from '../../config/db.js';
export async function createReview(req,res){const {rating,comment}=req.body;if(!rating)return res.status(400).json({message:'Rating is required'});const registered=await sql`SELECT id FROM event_registrations WHERE event_id=${req.params.id} AND user_id=${req.user.user_id} LIMIT 1`;if(!registered[0])return res.status(403).json({message:'Register for the event before reviewing'});try{const rows=await sql`INSERT INTO reviews(event_id,user_id,rating,comment) VALUES(${req.params.id},${req.user.user_id},${Number(rating)},${comment||null}) RETURNING *`;res.status(201).json({message:'Review submitted',review:rows[0]});}catch(e){if(e.code==='23505')return res.status(409).json({message:'You already reviewed this event'});throw e;}}
